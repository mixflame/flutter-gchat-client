name: draw
description: Flutter Application to draw on screen.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^5.0.0
  rxdart: ^0.23.1
  flutter_sodium: any
  equatable: ^1.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_colorpicker: any



flutter:

  uses-material-design: true

